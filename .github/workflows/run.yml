name: arXiv data refresh disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain upstream data source
        run: |
          echo "This fork does not crawl or process papers."
          echo "The website reads data directly from:"
          echo "https://github.com/huangpipip/daily-arXiv-ai-enhanced/tree/data"
          echo "No OPENAI_API_KEY, OPENAI_BASE_URL, or model variables are required for data refresh in this fork."
